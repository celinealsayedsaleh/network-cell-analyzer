<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-feature android:name="android.hardware.telephony" android:required="true"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher" android:label="@string/app_name" android:roundIcon="@mipmap/ic_launcher_round" android:supportsRtl="true" android:theme="@style/Theme.MyApplication" android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.WelcomeActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- HomeActivity — navigation hub -->
        <activity
            android:name=".ui.HomeActivity"
            android:exported="false"
            android:parentActivityName=".ui.WelcomeActivity"/>

        <!-- MainActivity — live monitor (no longer the launcher) -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:parentActivityName=".ui.HomeActivity"
            android:label="Live Monitor"/>

        <!-- MyStatsActivity — replaces StatsActivity -->
        <activity
            android:name=".ui.MyStatsActivity"
            android:exported="false"
            android:parentActivityName=".ui.HomeActivity"
            android:label="My Statistics"/>

        <!-- Keep your existing SettingsActivity as-is -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.HomeActivity"/>

        <!-- Keep your CellMonitorService as-is -->
        <service
            android:name=".service.CellMonitorService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice"/>
    </application>
</manifest>